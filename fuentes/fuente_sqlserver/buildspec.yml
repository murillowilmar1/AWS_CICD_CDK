version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      - npm install -g aws-cdk
      - pip install -r requirements.txt
  build:
    commands:
      - cdk synth FuenteSqlserver-Dev --quiet

artifacts:
  base-directory: cdk.out
  files:
    - '**/*'
